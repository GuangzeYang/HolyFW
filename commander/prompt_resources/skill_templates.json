{
  "exchange-use": {
    "name": "exchange-use",
    "when": "Use when the role must send, view, reply, forward, search, or file Exchange mail.",
    "template": "Use the exchange-use skill, open the Exchange mailbox, <action>, {<field>: <value>, ...}",
    "params": "JSON-like object: no quotation marks around keys or values. Omit unused keys. Omit the {...} block when the action has no fields.",
    "rules": [
      "target defaults to first email when omitted on view, reply, reply all, forward, delete, or flag.",
      "recipient, cc, and bcc must be a lab role short name (manager, hr, accountancy, programmer) or the matching *@ndrtest.local address. Do not send to the current role.",
      "Do not invent mailboxes outside the four lab roles.",
      "For send email, reply, reply all, forward, and save draft: set min_words to an integer from 300 to 800. Do not write a long body; at most one short outline sentence. The soldier expands the prose.",
      "Do not put min_words on view, search, delete, move, flag, mark unread, or recipient/subject/path fields."
    ],
    "actions": [
      {
        "name": "send email",
        "required": ["recipient", "subject", "min_words"],
        "optional": ["body", "cc", "bcc", "attachment"]
      },
      {
        "name": "view email",
        "required": [],
        "optional": ["target", "folder"]
      },
      {
        "name": "reply",
        "required": ["min_words"],
        "optional": ["body", "target", "cc"]
      },
      {
        "name": "reply all",
        "required": ["min_words"],
        "optional": ["body", "target", "cc"]
      },
      {
        "name": "forward",
        "required": ["recipient", "min_words"],
        "optional": ["target", "body", "cc"]
      },
      {
        "name": "search",
        "required": ["query"],
        "optional": ["target"]
      },
      {
        "name": "delete",
        "required": [],
        "optional": ["target", "folder"]
      },
      {
        "name": "move",
        "required": ["folder"],
        "optional": ["target"]
      },
      {
        "name": "flag",
        "required": [],
        "optional": ["target"]
      },
      {
        "name": "mark unread",
        "required": [],
        "optional": ["target"]
      },
      {
        "name": "save draft",
        "required": ["min_words"],
        "optional": ["body", "recipient", "subject"]
      },
      {
        "name": "open calendar",
        "required": [],
        "optional": []
      },
      {
        "name": "open people",
        "required": [],
        "optional": []
      },
      {
        "name": "open tasks",
        "required": [],
        "optional": []
      }
    ]
  },
  "odoo-use": {
    "name": "odoo-use",
    "when": "Use when the role must work in Odoo (Employees, Recruitment, Discuss, Calendar, Contacts, Surveys, To-do).",
    "template": "Use the odoo-use skill, log in to the Odoo system, use the <module> module, <operation>, {<field>: <value>, ...}",
    "params": "JSON-like object: no quotation marks around keys or values. Omit unused keys. Omit the {...} block when the operation has no fields.",
    "rules": [
      "Do not mention playwright-browser in the task string; the odoo-use skill loads it.",
      "Use only listed module and operation names.",
      "Create and add operations need a unique name or job position; do not reuse a fixed demo name.",
      "For post message: set min_words to an integer from 300 to 800. Do not write a long body; at most one short outline sentence. The soldier expands the prose."
    ],
    "actions": [
      {
        "module": "Employees",
        "name": "search employee",
        "required": [],
        "optional": ["name", "query"]
      },
      {
        "module": "Employees",
        "name": "open employee",
        "required": ["name"],
        "optional": []
      },
      {
        "module": "Employees",
        "name": "add employee",
        "required": ["name"],
        "optional": ["job position", "work email", "work phone", "work mobile", "department", "manager", "tags"]
      },
      {
        "module": "Employees",
        "name": "update employee",
        "required": ["name"],
        "optional": ["job position", "work email", "work phone", "work mobile", "department", "manager", "tags"]
      },
      {
        "module": "Employees",
        "name": "delete employee",
        "required": ["name"],
        "optional": []
      },
      {
        "module": "Employees",
        "name": "view departments",
        "required": [],
        "optional": []
      },
      {
        "module": "Recruitment",
        "name": "create job posting",
        "required": ["job position"],
        "optional": ["email address"]
      },
      {
        "module": "Recruitment",
        "name": "update job posting",
        "required": ["job position"],
        "optional": ["department", "email address"]
      },
      {
        "module": "Recruitment",
        "name": "delete job posting",
        "required": ["job position"],
        "optional": []
      },
      {
        "module": "Recruitment",
        "name": "view applications",
        "required": [],
        "optional": ["job position"]
      },
      {
        "module": "Recruitment",
        "name": "create applicant",
        "required": ["name"],
        "optional": ["job position", "email", "phone"]
      },
      {
        "module": "Discuss",
        "name": "read inbox",
        "required": [],
        "optional": []
      },
      {
        "module": "Discuss",
        "name": "post message",
        "required": ["min_words"],
        "optional": ["body", "channel", "topic"]
      },
      {
        "module": "Calendar",
        "name": "view calendar",
        "required": [],
        "optional": []
      },
      {
        "module": "Calendar",
        "name": "create event",
        "required": [],
        "optional": ["title", "attendee"]
      },
      {
        "module": "Contacts",
        "name": "add contact",
        "required": [],
        "optional": ["name", "email", "phone"]
      },
      {
        "module": "Surveys",
        "name": "view surveys",
        "required": [],
        "optional": []
      },
      {
        "module": "To-do",
        "name": "view tasks",
        "required": [],
        "optional": []
      }
    ]
  },
  "smb-access": {
    "name": "smb-access",
    "when": "Use when the role must create, view, update, copy, move, rename, or delete files on the company SMB share.",
    "template": "Use the smb-access skill, connect to the SMB shared directory, use <op> to <detail>, {<field>: <value>, ...}",
    "params": "JSON-like object: no quotation marks around keys or values. Omit unused keys.",
    "rules": [
      "Use POSIX paths that start with /Company_Data/. Do not emit UNC paths in the task string.",
      "Every path must stay under an Allowed SMB tree or SMB task path listed in context.env for this role.",
      "Do not write to another role's private folder.",
      "<detail> is a short phrase that restates the op (view a folder, create a file, download a file). Do not add extra semantics.",
      "For create file prefer a .docx path with topic and min_words. The soldier writes a Word document about that topic and uploads it to the share. Do not write the document body in the task string.",
      "For create file, append, and update file: set min_words to an integer from 300 to 800. Do not write a long content value; at most one short topic sentence.",
      "create file write paths should end in .docx (preferred), or .txt, .md, or .csv. append and update file stay on .txt, .md, or .csv.",
      "Use copy to place a .docx onto Exchange or Public (share-to-share). Use download to copy a share file onto the local Desktop."
    ],
    "actions": [
      {
        "name": "view",
        "required": ["path"],
        "optional": []
      },
      {
        "name": "create folder",
        "required": ["path"],
        "optional": []
      },
      {
        "name": "create file",
        "required": ["path", "min_words", "topic"],
        "optional": ["content"]
      },
      {
        "name": "append",
        "required": ["path", "min_words", "topic"],
        "optional": ["content"]
      },
      {
        "name": "update file",
        "required": ["path", "min_words", "topic"],
        "optional": ["content"]
      },
      {
        "name": "copy",
        "required": ["source path", "destination path"],
        "optional": []
      },
      {
        "name": "download",
        "required": ["path"],
        "optional": ["local path"]
      },
      {
        "name": "move",
        "required": ["source path", "destination path"],
        "optional": []
      },
      {
        "name": "rename",
        "required": ["path", "new name"],
        "optional": []
      },
      {
        "name": "delete",
        "required": ["path"],
        "optional": []
      }
    ]
  },
  "playwright-browser": {
    "name": "playwright-browser",
    "when": "Use when the role must browse the public web or visit a non-OWA, non-Odoo URL. Do not use for Exchange or Odoo.",
    "template": "Use the playwright-browser skill, open the browser, then execute: 1. <op>[, {key: value}] 2. <op>[, {key: value}] ... Verify: <observable result> Close the browser after verification.",
    "params": "JSON-like objects: no quotation marks around keys or values. Shared optional fields: name, role, nth, url, query, text, key, path, direction, amount, timeout_seconds.",
    "rules": [
      "Write the entire numbered list on one line inside the task string. Do not insert raw newlines.",
      "Every playwright task must include numbered ops, a Verify: clause, and Close the browser after verification.",
      "Never use Google. Never open OWA or Odoo URLs with this skill.",
      "After search, prefer follow with nth, then scroll, extract, wait, or back.",
      "Do not invent in-page control names for unknown public sites.",
      "Do not emit REPLACE_ tokens or other placeholders."
    ],
    "ops": [
      "goto",
      "search",
      "click",
      "type",
      "fill",
      "scroll",
      "wait",
      "select",
      "press",
      "check",
      "uncheck",
      "upload",
      "download",
      "extract",
      "follow",
      "hover",
      "back",
      "forward",
      "reload",
      "new tab"
    ],
    "actions": [
      {
        "name": "goto",
        "required": ["url"],
        "optional": []
      },
      {
        "name": "search",
        "required": ["query"],
        "optional": ["nth"]
      },
      {
        "name": "follow",
        "required": [],
        "optional": ["nth", "name", "query"]
      },
      {
        "name": "scroll",
        "required": [],
        "optional": ["direction", "amount", "name"]
      },
      {
        "name": "extract",
        "required": [],
        "optional": ["name"]
      },
      {
        "name": "wait",
        "required": [],
        "optional": ["timeout_seconds", "name"]
      },
      {
        "name": "back",
        "required": [],
        "optional": []
      },
      {
        "name": "click",
        "required": [],
        "optional": ["name", "role", "nth"]
      }
    ]
  }
}
